using Auth.Service.Commands;
using System.Threading;
using System.Threading.Tasks;
using Xunit;


namespace Auth.Service.Tests
{
    public class RegisterCommandHandlerTests
    {

        
        //[Fact]
        //public async Task ShouldReturnFalseWhenPasswordLenghtDoesNotMeetRequirement()
        //{
            //RegisterCommandHandler handler = new(8);
            //RegisterCommand command = new("someone@abc.com", "abc","abc");

            //var tokenSource = new CancellationTokenSource();
            //var token = tokenSource.Token;

            //var result = await handler.Handle(command, token);

            //result.Should().NotBeNull();

        //}
    }
}